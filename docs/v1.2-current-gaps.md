# v1.2 Current Gaps

> Last updated: 2026-05-01
>
> This note tracks what remains before a `v1.2.0-preview` release. It should stay short and factual.

## Already Verified

- Version metadata is aligned across `package.json`, README, and `CHANGELOG.md`.
- README keeps the project positioned as `MVP / 开源孵化`.
- Security defaults remain conservative:
  - `observe.attach_mode=manual`
  - `observe.allow_auto_chat=false`
  - `tools.enabled=false`
  - `tools.allow_shell=false`
- Full automated test suite passes:
  - `python -m pytest`
  - `python scripts/check_python_syntax.py`
  - `python scripts/check_js_syntax.py`
  - `python scripts/check_secrets.py`
- Character Runtime compatibility and metadata behavior are covered by automated tests.
- Manual smoke test on 2026-05-01 confirmed:
  - Electron desktop window starts.
  - Live2D displays in the expected position.
  - Text chat can produce a reply with a working LLM provider.
  - Live2D mouth/motion responds while speaking.
  - Browser TTS produces audible speech.
- Demo recording on 2026-05-01 confirmed:
  - GPT-SoVITS local voice path can be used for higher-quality recording.
  - Demo material was recorded successfully and accepted by the maintainer.

## Remaining Manual Checks

- First-run install flow from a clean checkout:
  - `pip install -r requirements.txt`
  - optional `pip install -r requirements-dev.txt`
  - `npm install`
- Electron desktop startup through `start_electron.bat` or `npm run start:electron`.
- Live2D model load with a documented `model_path`.
- Server TTS failure fallback behavior, if a server TTS provider is configured.
- Character Runtime demo path with local opt-in:
  - `character_runtime.enabled=true`
  - `character_runtime.return_metadata=true`
- Cross-window Character Runtime forwarding to the model window.
- Demo capture readiness with no secrets or private desktop content visible.

## Release Blockers Before Preview

- Current working tree still contains unrelated local changes that should be reviewed before tagging.

## Suggested Next Step

Review the working tree before tagging and keep local demo configuration out of the release commit.
